// Deprecated. The new app uses individual server-component pages under
// `src/app/app/{parent,teacher,admin}/*/page.tsx` which fetch live data from
// the API. This file is kept only to avoid breaking any external imports.

export function RouteDashboard() {
  return null;
}
